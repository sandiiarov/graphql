// @flow

/**
 * These types are intended only for usage in tests.
 *
 * They SHOULD NOT be used outside of the test scope.
 */

export type SimplifiedGraphQLFieldMap = {
  [fieldName: string]: Object,
};
